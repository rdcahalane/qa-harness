# qa-harness

This repository contains scripts to automate common tasks for maintaining quality and consistency across projects.

## Usage

To integrate the qa-harness into a project, run the following command:

```bash
./integrate.sh
```